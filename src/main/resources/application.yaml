spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/assignment_db
    username: postgres
    password:
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true